node_modules/
dist/
.env.local
.env.*.local
sessions/
*.log
# Local notes with secrets — never commit
DesiZoom.txt
supabase/.temp/

# Database dumps — contain real customer data, never commit
backups/
backup_prelaunch.sql
